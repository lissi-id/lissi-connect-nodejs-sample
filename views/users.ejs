<!DOCTYPE html>
<html>
  <head>
    <title>User Details</title>
    
  </head>
  <body>
    <div class="content">
      <h1>Welcome <em><%= user["First Name"] %></em></h1>
      <br />

      <p>Here are your user details:</p>
      
      <p>
        <% Object.keys(user).forEach((item) => { %>
          <b><%- item %></b>: <%- user[item] %>
          <br />
        <% }); %>
      </p>

      <form action="/auth/logout" method="post">
        <input type="submit" value="Logout">
      </form>
    </div>
  </body>
</html>